{% extends 'base.html.twig' %}

{% block title %}UserMan{% endblock %}

{% block body %}
    <h1>UserMan</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_man.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ user_man.Username }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user_man.roles ? user_man.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user_man.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_man_index') }}">back to list</a>

    <a href="{{ path('user_man_edit', {'id': user_man.id}) }}">edit</a>

    {{ include('user_man/_delete_form.html.twig') }}
{% endblock %}
